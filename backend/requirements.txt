# ============================================================
# Backend dependencies — Resume Screening & Intelligence System
# Pinned for reproducibility. See comments for compatibility notes.
# ============================================================

# --- Web Framework ---
fastapi==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9        # multipart/form-data for file uploads

# --- Pydantic / Settings ---
pydantic==2.7.4
pydantic-settings==2.3.4

# --- Database ---
sqlalchemy==2.0.31
alembic==1.13.2

# --- Authentication ---
python-jose[cryptography]==3.3.0   # JWT
passlib==1.7.4                     # kept for API compat; hashing done with bcrypt directly
bcrypt==4.2.1                      # direct bcrypt; passlib 1.7.4 incompatible with bcrypt 5.x

# --- PDF Extraction ---
pdfminer.six==20231228

# --- NLP ---
spacy==3.7.5                       # en_core_web_lg downloaded separately:
                                   #   python -m spacy download en_core_web_lg

# --- ML ---
scikit-learn==1.5.1
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2                      # model serialization (ships with sklearn, pinned explicitly)

# --- Plotting / Analytics (used by analytics API) ---
plotly==5.22.0

# --- Email ---
aiosmtplib==3.0.1                  # async SMTP
jinja2==3.1.4                      # email templates (also used by alembic/fastapi)

# --- Utilities ---
python-dotenv==1.0.1
httpx==0.27.0                      # async HTTP client (tests + inter-service)
aiofiles==23.2.1                   # async file I/O for upload handling
python-magic-bin==0.4.14; sys_platform == "win32"   # MIME type detection (Windows)
python-magic==0.4.27; sys_platform != "win32"       # MIME type detection (Linux/Mac)

# --- Rate Limiting ---
slowapi==0.1.9                     # Starlette-native rate limiter

# --- Testing ---
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
